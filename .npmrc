# First-party @cinatra-ai/* packages live in the Cinatra marketplace
# registry, not npmjs. Everything else still resolves from npmjs.
@cinatra-ai:registry=https://registry.cinatra.ai
//registry.cinatra.ai/:_authToken=${CINATRA_REGISTRY_TOKEN}
always-auth=true
